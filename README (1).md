# SkillProof Frontend

The `index.html` in this folder is the final SkillProof UI with the existing features and design preserved.

Run locally by opening `index.html` in a modern browser, or serve this folder with any static web server.
